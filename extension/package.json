{
  "name": "rabbithole-extension",
  "version": "0.4.0",
  "description": "RabbitHole — AI learning companion (Chrome extension)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:nocheck": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
